# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
pgvector==0.3.2
alembic==1.13.2
pydantic-settings==2.5.2
httpx==0.27.2
numpy==1.26.4

# M2
yt-dlp==2024.9.27
opencv-python-headless==4.10.0.84

# M3
line-bot-sdk==3.14.5

# M4
rapidfuzz==3.9.7

# i18n: 繁→簡中文機械轉換（scripts/translate_attractions.py 用）
opencc-python-reimplemented==0.1.7

# M1, M2, M3, M5, M7 (Gemini: vision + reason + itinerary)
google-genai==0.8.0

# M1+M3+M5 共用 embedder：本機跑 sentence-transformer，避開 Gemini free-tier RPM 限制
sentence-transformers==3.0.1

# M1 dump bootstrap：從 Hugging Face dataset 拉預算好的 attractions embedding，
# 讓隊友 docker 啟動時不必本機重 embed
huggingface_hub==0.24.6

# M7
apscheduler==3.10.4

# Test
pytest==8.3.3
pytest-asyncio==0.24.0
